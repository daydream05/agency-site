import {
  breakpoints,
  colors,
  lineHeights,
  fontSizes,
  space,
  maxWidth,
  mediaQueries,
  headerHeight,
} from "./tokens"

import { buttonStyles } from "./styles"

export default {
  breakpoints,
  colors,
  lineHeights,
  fontSizes,
  space,
  maxWidth,
  mediaQueries,
  headerHeight,
  buttons: buttonStyles,
}
